local p = game:GetService("Players").LocalPlayer
local h = (p.Character or p.CharacterAdded:Wait()):WaitForChild("Humanoid")
local b = p:WaitForChild("Backpack")

while true do
	local t = b:FindFirstChildOfClass("Tool")
	if t then h:EquipTool(t) wait(2) h:UnequipTools() end
	wait(10)
end
